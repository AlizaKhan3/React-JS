import React from 'react';
import { Button, Flex } from 'antd';

const AppButton = () => (
      <><Button type="primary">Primary</Button><Button type="primary" disabled>
        Primary(disabled)
    </Button></>
);
export default AppButton;